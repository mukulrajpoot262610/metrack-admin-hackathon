import React, { useState } from 'react'

const Header = ({ settings }) => {

    return (
        <div className='rounded-lg w-full'>
            <h1 className='font-bold text-3xl capitalize'><span className='text-white uppercase'>ME-track Dashboard</span></h1>
        </div>
    )
}

export default Header